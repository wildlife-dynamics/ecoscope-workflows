{% import 'call-method-macros.jinja' as call_method_macros %}
# ruff: noqa: E402

# %% [markdown]
# # {{ spec.id.title().replace("_", " ") }}
# TODO: top level description

# %% [markdown]
# ## Imports

import os
{% for t in spec.workflow -%}
{{ t.known_task.importable_reference.statement }}
{% if loop.last %}
{% endif -%}
{% endfor -%}
{%- for t in spec.workflow -%}
# %% [markdown]
# ## {{ t.name }}

# %%
# parameters

{{ t.id }}_params = {{ t.known_task.importable_reference.params_notebook }}

# %%
# call the task
{% set params = t.id ~ '_params' %}
{% set indent = '' %}
{{ call_method_macros.handle_task(t, params, indent) }}
{% endfor -%}
